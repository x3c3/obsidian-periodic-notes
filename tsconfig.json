{
  "extends": "@tsconfig/svelte/tsconfig.json",
  "include": ["src/**/*", "version-bump.ts"],
  "exclude": ["src/**/*.test.ts"],
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["DOM", "ESNext"],
    "module": "ESNext",
    "types": ["svelte", "node"],
    "noEmit": true,
    "baseUrl": ".",
    "paths": { "src/*": ["src/*"] }
  }
}
